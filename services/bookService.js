import { booksJsonResponse } from "../apihelpers/bookApi.js";
import { bookModel } from "../models/bookModel.js";

export const updateOLID = async (req,res) => {

  try{

  } catch(error) {
    console.error("Unable to compare the title and updatedTitle",error);
  }
}
